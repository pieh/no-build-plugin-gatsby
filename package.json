{
  "name": "gatsby-starter-hello-world",
  "private": true,
  "description": "A simplified bare-bones starter for Gatsby",
  "version": "0.1.0",
  "license": "0BSD",
  "scripts": {
    "build": "VERBOSE=1 gatsby build",
    "develop": "gatsby develop",
    "start": "gatsby develop",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "test": "cd plugins/gatsby-adapter-netlify && yarn && yarn build && cd ../.. && yarn clean && rm -rf .netlify && yarn build"
  },
  "dependencies": {
    "gatsby": "^5.10.0-alpha-adapters.130",
    "gatsby-adapter-netlify": "^1.0.0-alpha-adapters.21602",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/gatsbyjs/gatsby-starter-hello-world"
  },
  "bugs": {
    "url": "https://github.com/gatsbyjs/gatsby/issues"
  }
}
